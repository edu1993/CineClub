/**
 * Array en memoria para almacenar reseñas mientras la aplicación está
 * en ejecución. Se exporta como variable mutable para que los tests puedan
 * reiniciarlo entre casos.
 */
export let reviews = [];
